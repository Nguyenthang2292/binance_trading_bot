Windows setup for this repo: use VS Build Tools bundled CMake at `C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe`; OpenSSL installed via winget at `C:\Program Files\OpenSSL-Win64`; CMake preset `windows-msvc-debug` is the default workspace preset; build reaches existing `src/orders/orders.cpp` / `orders.h` compile errors, so environment is ready but repo still needs code fixes for a full Windows build.